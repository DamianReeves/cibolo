{
  "name": "cibolo-cli",
  "version": "0.1.0",
  "description": "CLI for Cibolo document translation tool",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "cibolo": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@trpc/server": "^10.45.2",
    "trpc-cli": "^0.1.0",
    "arktype": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}

